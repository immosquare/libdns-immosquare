# Codex Instructions

## Source of Truth

Read `CLAUDE.md` in this directory before doing any work. Treat it as the source of truth for this project's architecture, commands, conventions, and workflow.

For shared immosquare rules, commands, agents, and skills, use `~/.claude/` as the single source of truth. Do not duplicate those rules into project-specific Codex files.

## Working With Claude Rules

Follow the local Claude guidance as closely as possible. When Claude-specific features are mentioned, adapt execution details for Codex while preserving the intent and constraints.

If there is any conflict:
1. the project `CLAUDE.md` wins for project-specific behavior;
2. `~/.claude/` wins for shared immosquare rules and workflows.
